# Seconds to wait for NX under-test to start-up
nexus.launcher.startTimeout=300

# HACK: Used only by ruby tests ATM, pending refactoring or keeping this as general configuration
nexus.launcher.bundleCoordinates=${it.nexus.bundle.groupId}:${it.nexus.bundle.artifactId}:zip:bundle:${it.nexus.bundle.version}