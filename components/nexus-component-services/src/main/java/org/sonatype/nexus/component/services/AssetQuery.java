package org.sonatype.nexus.component.services;

// Similar to ComponentQuery, but can probably assume a specific list of
// attributes (those defined by the Asset interface) can be constrained/involved in the query.
public class AssetQuery
{
}
